node_modules/
dist/
.env
.env.*
!.env.example
data/*.db
data/*.db-*
data/artifacts/
data/documents/
# personal data lives only locally — never in the (public) repo
data/*.seed.json
data/out/
*.log
.DS_Store

# CV kit: the filled template, the portrait and the rendered PDFs are personal
cv/cv-template.html
cv/photo.jpg
cv/out/
